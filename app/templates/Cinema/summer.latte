{var $title="Letní kina"}

{block #content}
<h1 class="page-header">{$title} 
	<small>
		(<a href="http://letnaky.zitbrno.cz" title="Celkový program letních kin.">{fa calendar} program</a>)
	</small>
</h1>
{php $cinemas = new \Zitkino\Cinemas(); }

<ul class="list-group">
	{import "list.latte"}
	{foreach $cinemas->getSummer() as $cinemaItem}
		{include #cinemaList, cinema => $cinemaItem->getData()}
	{/foreach}
</ul>
{/block}
